Execute implementation strictly according to tasks.md, preserving the staged sequence, architectural decomposition, and inseparable code-and-test development required by the constitution. 

Implement tasks in small, reviewable increments and commit as often as meaningfully possible. At minimum, after successfully completing each task or each GitHub issue, stage the relevant changes, create a focused commit, and push to `origin`. Do not batch unrelated completed tasks into a single commit when they can be committed independently.

Actively use GitHub issue tracking throughout implementation. For each task you work on, link the work to the corresponding GitHub issue, keep the issue status/body/checklists updated as progress is made, and reflect any important implementation notes or validation results in the issue. After a task/issue is successfully completed and validated, close the corresponding GitHub issue.

Every behavior-changing implementation step MUST include the corresponding test work in the same implementation unit. Do not defer required tests, optimality validation, or edge-case coverage to later follow-up.

Mark tasks complete in tasks.md only after the implementation, tests, commit, push, and issue update/closure steps are done for that task. Keep commits focused, traceable, and aligned with task IDs / issue IDs where possible.

Report progress in terms of completed tasks, commits created, pushes performed, and GitHub issues updated or closed.
